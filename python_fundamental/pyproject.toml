[project]
name = "python-fundamental"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Surendra Shukla", email = "surendra.shukla29@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyterlab>=4.6.3",
]

[project.scripts]
python-fundamental = "python_fundamental:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
