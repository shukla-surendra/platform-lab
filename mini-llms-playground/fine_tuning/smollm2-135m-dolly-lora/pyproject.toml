[project]
name = "smollm2-135m-dolly-lora"
version = "0.1.0"
description = "LoRA instruction-tuning of a BASE (non-chat) SmolLM2-135M on Dolly-15k, with a before/after comparison"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.2",
    "transformers>=4.44",
    "peft>=0.12",
    "datasets>=2.19",
    "accelerate>=0.33",
    "tqdm>=4.66",
]

[tool.uv]
package = false
