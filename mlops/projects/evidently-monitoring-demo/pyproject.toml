[project]
name = "evidently-monitoring-demo"
version = "0.1.0"
description = "Runnable Evidently + XGBoost drift/performance monitoring demo, logged to MLflow"
requires-python = ">=3.11"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "xgboost",
    "evidently",
    "mlflow",
    "jupyter",
    "ipykernel",
]
