# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# All run artifacts -- data/, models/, and reports/ are entirely
# regenerated by generate_reference_data.py / train.py / monitor.py.
# Nothing in this project's pipeline output is committed, same reasoning
# as ../fraud-detection-xgboost/.gitignore: reproducible from code, not
# source.
data/
models/
reports/
mlruns/
mlflow.db
