# Optional — every variable has a default. Copy to terraform.tfvars to override.
# terraform.tfvars is gitignored (cloud-practice/.gitignore).

region        = "us-east-1"
project       = "cicd-practice-v2"
branch_name   = "main"
instance_type = "t3.micro"
