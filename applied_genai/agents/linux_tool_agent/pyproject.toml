[project]
name = "linux-tool-agent"
version = "0.1.0"
description = "A LangGraph + OpenAI tool-calling agent that answers real questions about this machine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.4.1",
    "langgraph>=1.2.11",
    "langchain-openai>=0.2",
    "langgraph-checkpoint-sqlite>=2.0",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
