# Keep the build context tiny — target/ alone is hundreds of MB and would be
# uploaded to the daemon on every build only to be ignored by the Dockerfile.
target/
data/
*.db
*.db-wal
*.db-shm
.git/
.gitignore
.DS_Store
README.md
Makefile
docker-compose.yml
