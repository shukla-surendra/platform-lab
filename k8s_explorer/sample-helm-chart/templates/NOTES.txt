{{ .Release.Name }}-nginx deployed — {{ .Values.replicaCount }} replica(s) of {{ .Values.image.repository }}:{{ .Values.image.tag }}

Look at it locally:

  kubectl port-forward svc/{{ .Release.Name }}-nginx 8080:{{ .Values.service.port }}
  open http://localhost:8080
