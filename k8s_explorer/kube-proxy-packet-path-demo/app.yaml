apiVersion: apps/v1
kind: Deployment
metadata:
  name: packet-path-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: packet-path-demo
  template:
    metadata:
      labels:
        app: packet-path-demo
    spec:
      containers:
        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: packet-path-demo
spec:
  selector:
    app: packet-path-demo
  ports:
    - port: 80
      targetPort: 80
