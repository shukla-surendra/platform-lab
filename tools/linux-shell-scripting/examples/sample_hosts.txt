# One URL per line. Lines starting with # are skipped (see the `grep -v` in parallel_health_check.sh).
# A mix of a healthy endpoint, a guaranteed 404, and an unreachable host —
# so a first run shows both the OK and FAIL paths.
https://example.com
https://example.com/definitely-not-a-real-path-xyz
https://this-host-does-not-exist.invalid/
